package com.server.service;

public class AuctionServiceTest {

    //UserRepository mockRepo = Mockito.mock(UserRepository.class);
    //Mockito.when(mockRepo.getUserByUsername("admin")).thenReturn(new Admin(...));

    //UserService userService = new UserService(mockRepo); // Truyền Mock vào
    //String result = userService.getUserProfile("admin");
// Dùng AssertJ hoặc JUnit assertions để check result có chữ "SUCCESS" không
}
